% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmix2norm.R
\name{dmix2norm}
\alias{dmix2norm}
\alias{pmix2norm}
\alias{qmix2norm}
\title{Bivariate Gaussian mixture distribution}
\usage{
dmix2norm(
  x,
  means = matrix(0, 4, 2),
  Sigma = rep(list(diag(1, 2)), 4),
  probs = rep(1/4, 4),
  log = FALSE
)

pmix2norm(
  x,
  means = matrix(0, 4, 2),
  Sigma = rep(list(diag(1, 2)), 4),
  probs = rep(1/4, 4),
  log = FALSE
)

qmix2norm(
  p,
  means = matrix(0, 4, 2),
  Sigma = rep(list(diag(1, 2)), 4),
  probs = rep(1/4, 4),
  v = NULL,
  x0 = c(-1, 1),
  range_s = c(-100, 100),
  log.p = FALSE,
  lower.tail = TRUE
)
}
\arguments{
\item{x}{Numeric matrix. Rows are observations and columns are the two
locations/components.}

\item{means}{Numeric matrix of component means.}

\item{Sigma}{List of variance-covariance matrices.}

\item{probs}{Numeric vector of joint state probabilities.}

\item{log}{Logical. If \code{TRUE}, \code{dmix2norm()} or \code{pmix2norm()} returns values
on the log scale.}

\item{p}{Numeric vector of probabilities.}

\item{v}{Optional numeric direction vector used by \code{qmix2norm()}.}

\item{x0}{Numeric reference point used by \code{qmix2norm()}.}

\item{range_s}{Numeric vector of length two giving the search interval for
the scalar root used by \code{qmix2norm()}.}

\item{log.p}{Logical. If \code{TRUE}, probabilities are supplied on the log scale.}

\item{lower.tail}{Logical. If \code{TRUE}, probabilities are \eqn{P[X \le x]};
otherwise, \eqn{P[X > x]}.}
}
\value{
\itemize{
\item \code{dmix2norm()} returns a numeric vector of density values.
\item \code{pmix2norm()} returns a numeric vector of probabilities.
\item \code{qmix2norm()} returns a numeric matrix of directional quantiles.
}
}
\description{
Density, distribution function, and directional quantile approximation for a
bivariate Gaussian mixture with four joint states.
}
\details{
Version 1.0.0.
}
\examples{
x <- matrix(c(-1, 0, 0, 1), ncol = 2, byrow = TRUE)
dmix2norm(x)
pmix2norm(x)
qmix2norm(c(0.25, 0.75))

}
\keyword{distributions}
